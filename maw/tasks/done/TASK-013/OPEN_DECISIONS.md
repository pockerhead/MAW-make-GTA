- 2026-09-24: planner defaults accepted (TASK_FINAL.md).
- 2026-09-24: PR1 (pause_request against pending death/arrest transitions, shader arrow sign, raster preconditions) + PR2 accepted. PR2 reverted R11 (projection back in citygen per GDD §12 headless gates), fixed G3 so it compiles and runs (raise_heat before spawn_unit; Strike is pub(super)) and made its flips non-vacuous (dirty each reset row first).
- 2026-09-24: implementer a1 stopped when the Claude Code session ended (WIP committed at 6142115: sim + client done, QA scripts pending). Owner said 'Продолжаем' → continuation implementer spawned on the checkpoint.
- 2026-09-24: code review PASS with minors. Fixing now (cheap, owner-visible): (1) missing '⇄' glyph in Inter on toggle buttons → a glyph present in both Inter fonts, checked by a font-coverage gate over every UI string/literal; (2) move the hard-coded button labels/formats into strings.ron; (4) +/- at a range bound must not mark settings changed. (3),(5),(6) accepted as-is.
- 2026-09-24: QA SHIP-PENDING-RUNTIME accepted as SHIP. Bug 1 (Tnua sensors orphaned on character despawn, ~1 entity/s growth, pre-existing) → follow-up TASK-025 small-fix before T13 (a leak must not reach T16 perf). Bugs 2-3 (float noise in volume toml, minimap glyph overlap at the rim) → owner checklist / T16 polish. Orchestrator deleted the QA-only settings dir (%LOCALAPPDATA%/…maw-make-gta.qa) holding probe values so later QA/showcase runs start from defaults.
- 2026-09-24: PCTX folded: Tnua sensor orphans → bevy-ecs; paused BRP holds + real OS input + .qa settings id → agents/qa.md.
