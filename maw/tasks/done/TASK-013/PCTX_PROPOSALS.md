
## 2026-09-24 (TASK-013, plan-reviewer-2) — gates domain
bevy_brp_extras 0.22.6 releases `send_keys` holds and mouse button/click holds on `Res<Time>` in `Update` (`keyboard/keys.rs:149-155`, `mouse/button.rs:114`, `mouse/click.rs:166`) = the VIRTUAL clock. While `Time<Virtual>` is paused (GameState::Paused) a key/button sent over BRP is never released; a second press of the same key during the pause produces no `just_pressed`. `type_text` is per-frame and unaffected. Proposed risk lesson: QA scenarios send each key at most once per pause and type into fields with `type_text`; resume is checked after an unpause, or by the owner run.

## 2026-09-24 (TASK-013, qa) — bevy-ecs / gates domains
bevy-tnua 0.32 spawns proximity-sensor entities with `with_related_entities::<TnuaSensorOf>` and `TnuaSensorsSet` has no `linked_spawn` (`bevy-tnua-0.32.0/src/sensor_sets.rs:98-107`, `bevy-tnua-physics-integration-layer-0.13.0/src/data_for_backends.rs:62-68`): despawning a character leaves an orphan entity with only `TnuaProximitySensor` (not reflected, no `Transform`, so BRP queries and the G3 `Transform` leak detector miss it). Seed 1 idle: ~1 orphan/s from civilian recycling. Proposed risk lesson: any character despawn path also despawns its `TnuaSensorsSet` targets; leak audits list components per entity (`world.list_components`), not only reflected queries.
QA technique: while `GameState::Paused`, drive menu clicks and keys with real OS input (Windows `SendInput` via ctypes, window found by `--window-title`), since BRP holds never release on the frozen virtual clock (`scratch/qa/osinput.py` in TASK-013).
