- 2026-09-24: premise wrapper reaped; orphan codex hit rust-lld 0xc0000142 and lost file writes (3rd time: TASK-011 premise, TASK-012 premise). Verdict PREMISE HOLDS taken from its final message. Provider switch from here: premise-challenge, plan-reviewer-1 and code-reviewer → claude opus (native subagents are not reaped as background shells and do not hit the codex sandbox linker failure). Cross-vendor review diversity is lost until memory pressure is gone.
- 2026-09-24: planner questions answered with its defaults (TASK_FINAL.md).
- 2026-09-24: PR1 (claude) + PR2 accepted. PR2 caught a plan-wide defect: terminal Leave + set_heat without a tick made cops spawned right after set_heat leave forever (would red P1..P6 on correct code) → raise_heat helper; also fixture geometry fixes (D6, P6), P9 flip proven vacuous and reworked, compile fix for Loadout equality, heat table row 179 corrected.
- 2026-09-24: code review NEEDS_WORK accepted in full. The plaza N1 'evidence' was a broken fixture (player placed inside the tower on the plaza block) — the navmesh data in IMPL_SUMMARY and the owner checklist line are void until re-measured. Fixer: plaza player on a sidewalk anchor + GATE BROKEN on a sunk player and re-measure N1; head_for re-plan hole fixed as reviewer suggests (walked && nearest_node(chest)==goal) with a gate that reds on revert (the change is currently ungated); bound the post-last-node straight segment; move queue_slot/pinned rays into the AI slot or justify the per-tick cost with a measurement; P9 doc comment.
- 2026-09-24: fixer accepted, incl. rejecting a length cap on the post-last-node segment (would re-create the oscillation) in favour of avoidance on it.
- 2026-09-24: QA NEEDS_FIXES accepted. Fixer round 2: (1) fire-line range boundary — blockers must be judged against the bullet's real reach (from muzzle, to the near capsule surface), shared code for gangs and police, deterministic boundary-pair gate + QA's 12-SWAT surround probe committed as a friendly-damage gate; (2) t11.py tracker counts a cop as arrived by real distance (<= keep_distance.1), not by FSM state. Not fixed here: idle SWAT at 5 stars (accepted 'single file' risk at scale; owner checklist), walking does not escape arrest (GDD-correct), camera inside a cop body on the Busted screen (owner checklist; camera polish belongs to T13 juice).
- 2026-09-24: fixer round 2 accepted (overreach = muzzle offset + max(capsule, head) radius, derived and conservative; boundary pair + 12-SWAT surround gates, flip-RED on 5 layouts). No QA round 2: the change is one condition in shared fire_line, covered by new deterministic gates plus all gang fire-line gates green, and t11 re-run PASS. SHIP. The 60 FPS reading in one run = inactive-window frame limiter (first run 144 FPS / 2.5 ms).
- 2026-09-24: PCTX folded: test-area collisions, raise_heat, distance-based trackers → gates; bullet-reach hold-fire + head_for re-plan rule → game-design.
