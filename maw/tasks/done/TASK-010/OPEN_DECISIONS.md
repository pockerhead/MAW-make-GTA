- 2026-09-24: planner open questions Q1-Q4 answered with the planner's recommended options (TASK_FINAL.md Resolved questions).
- 2026-09-24: PR2 accepted: V2 blocker #1 (schedule cycle) refuted by the civilian_death precedent; A* (pathfinding 4.16.0, GDD-approved) synchronous with route_requests_per_tick=2 per measured cost (11 us mean, <=215 us worst on seed 1); three new PLAN defects fixed in PLAN_FINAL (matrix-shot tautology, NPC fire during weapon swap, test player dying).
- 2026-09-24: code review NEEDS_WORK accepted in full (3 issues + 3 missing coverage). Orchestrator addition for issue 1: the T8 regression run in the same checkout also reads ~30 FPS, and this project already had a "30 FPS = 30 Hz display under vsync" false alarm (TASK-002) — the fixer must first establish present mode and monitor refresh and measure with AutoNoVsync before any optimization. Lethality stays an owner-feel item, gated for cadence only.
- 2026-09-24: fixer accepted. 30 FPS = host monitor \.\DISPLAY9 at 30 Hz under Fifo (again); real frame 2.4-2.6 ms with or without a firefight; Game.frame_report() now records refresh/present mode. Review issue 3 prescription refuted (re-instance legitimately drops the gun for 1-2 updates); gate rewritten with a forced re-instance. Possible friendly fire (member died mid-fight from own group's bullets) → QA must confirm and check whether it provokes/breaks the group.
- 2026-09-24: QA SHIP-PENDING-RUNTIME with friendly fire confirmed (7-23 % of member shots hit groupmates, 4/6 layouts lose a member in 30 s). Counted as T9 scope (the owner will see it in the first firefight) → fixer round 2. Decision: option (a) hold fire while a groupmate is inside the aim cone (segment widened by body radius + the spread cone at that distance) AND sidestep to clear the line, so members don't just freeze; NOT option (b) — bullets passing through allies reads as a bug and T11 police will reuse the same rule. Gate: QA probe layouts → 0 friendly hits and every member still fires (liveness); t9.py asserts every member ends at full HP. Also chain attach_held_gun → show_held_gun (1-frame hide).
- 2026-09-24: fixer round 2 accepted (hold-fire check extended to weapon range — a miss keeps flying; sticky sidestep side; punches covered). Circling-without-firing when groupmates melee next to the player → owner checklist. QA round 2 runs focused on the new behaviour.
- 2026-09-24: QA round 2 NEEDS_FIXES accepted, not downgraded to feel: bug 2 (a member can fire 0 shots all fight — mutual sidestep lock, sidestep into a wall, circling vs a standing player) is deterministic and T11 police reuse the rule. Fixer round 3, redesign instead of patching the sidestep: evaluate a small set of candidate positions (both sides at 2-3 offsets, plus a step back/forward) by the ACTUAL rotated line from that position to the target extended to weapon range, reject unwalkable ones via the avoidance rays, pick the nearest clear one, re-evaluate on every AI slot; deterministic tie-break between two members blocking each other (lower entity yields/moves, the other holds). Melee: re-check at impact, a friendly in the strike volume at impact time cancels the hit on the friendly (no damage to non-hostiles from gang punches). Gates: QA2 probe layouts incl. mutual lock, wall, circling-vs-standing-player → every ranged member fires >= N shots in 30 s, 0 friendly damage (guns and fists). If this round fails on the same class again, stop and simplify (per the TASK-018/019 lesson).
- 2026-09-24: fixer round 3 STOP accepted as designed. The remaining starving layout (gunman behind a flanking fist scrum) is geometry, not a bug: a body 1.56 m from the target closes every line past ~2.5 m. Decision: option (a) — a gunman whose only blockers are groupmates already fighting the same target holds `keep_distance` (does not close in and join a scrum it cannot hit through), re-evaluates each slot, fires the moment a line clears; the shielding-bystander case follows the same rule. Liveness for scrum layouts = group hits on the target over 30 s, not per-member shots; the #[ignore] gate is rewritten to that form and un-ignored. (b) rejected (breaks 0 friendly damage), (c) rejected (patching). Rewritten matrix gate (Swing.landed, 0 damage, no Attack) accepted; note the old provoke flip now reds only half 3.
- 2026-09-24: fixer round 4 accepted incl. its refinement: a 'yielding' blocker is any body pinned within melee_distance (2.5 m) of the target, not any engaged groupmate (the literal rule reds 5 gates — shooters legitimately close in to open lines past groupmates at range). Final independent QA round 3 on rounds 3-4 behaviour.
- 2026-09-24: final QA round 3 SHIP-PENDING-RUNTIME accepted as SHIP. Bug 1 (rear member in a <=3 m corridor never fires: close-in fallback walks into a non-pinned groupmate) is Minor, group pressure intact; carried as a binding note into TASK-012 (T11 police reuse the fallback). Shotgun gang lethality 1.5-3 s → owner checklist.
- 2026-09-24: PCTX folded: own RNG stream per NPC role + pathfinding Ord costs → bevy-ecs; GLB harness materials, re-instance of joint children, 30 Hz host FPS → gates; scrum/shield geometry + rotated line → game-design; QA corridor fallback → TASK-012 note.
