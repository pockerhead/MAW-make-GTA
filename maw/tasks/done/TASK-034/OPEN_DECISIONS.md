- 2026-09-26: code review PASS with one unverified minor (the ground pushes the camera at pitch ≥ ~57.6° of max 60°, so the player hides in the open while looking steeply up). Fixer skipped: nothing actionable. QA measures it in game. If QA confirms it and the model reappears only below ~45°, accept it as a data-tunable feel item for the owner ("look at the player while looking up at the sky"). The alternative was a separate ground-vs-wall rule, which is more machinery for a rare pose.
- 2026-09-26: QA confirmed the look-up hide: the model hides at 57-60° pitch and returns at 45°. Accepted as a data-tunable feel item. QA also noticed that the camera does not collide with cars, so a car body can hide the player. That predates this task and is left as an observation for the owner's run; no task yet.
