- 2026-09-26: implementer accepted — root cause was a circular wait (second cars holding grants behind ungranted heads) + cars stopping ON crosswalks (walkers and cars waiting on each other) + non-expiring grants, not the playtester's hypothesis. Gridlock gone on 4 seeds (worst 18.7-38.8 s vs 87-117 s).
- 2026-09-26: remaining saturation decision (AC2 amended): IDM acceleration 0.73 m/s² is unrealistically sluggish (a car holds the box 8.1 s from rest; IDM literature uses ~1.0-1.5, GTA traffic launches briskly) → set `idm.acceleration: 1.5`. AC2 becomes: no AI car stands > 40 s on seeds 1/2/7/42 (saturated queues near a busy junction are realistic traffic; the lease loop is excluded by the gate's grant-hold assertion). Whole-connector reservation vs GDD "conflict points" stays with TASK-032.
- 2026-09-26: code review NEEDS_WORK accepted (lease re-grant loop vs gate, lease assertion never RED alone, flips to re-run at a=1.5/40 s). Lease semantics decided: expires only in favour of a conflicting waiter.
- 2026-09-26: fixer — known residue (review issue 5): a grant holder standing on its connector or with its nose past the stop line keeps the lease indefinitely (e.g. behind a player-parked car in the box); pre-existing behaviour, recorded in TASK-032.
- 2026-09-26: fixer accepted (a=1.5, 40 s bound, contested lease with a deterministic gate, flips all RED). QA stage skipped: the runtime baseline was re-run by the fixer (jam signals 27→13, worst stand 25 s, 0 stuck walkers), CI green; remaining 20-25 s queues at the busy junction are saturation → the playtest continuation judges them. SHIP.
- 2026-09-26: PCTX folded (junction rules → game-design; stationary-player liveness gates → gates).
