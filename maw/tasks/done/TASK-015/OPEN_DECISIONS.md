- 2026-09-24: planner Q1-Q5 decided (Q2 changed from the default: bullets damage the car's health; see TASK_FINAL). Planner's latent R9 confirmed by the orchestrator in the vendored source (return instead of continue in apply_motors_system) → fixed within TASK-015 with a gate, because every corpse silently disables motors of later-iterated characters.
- 2026-09-24: PR1+PR2 accepted (vendored motor fix gate made non-vacuous via table order; G12(c) steep-shot fixture; SMG for the fire latch flip).
- 2026-09-24: implementer accepted incl. deviations (query-order precondition via pre-created QueryState, roll lever sign, exit ray filter, fixtures made falsifiable). Rolling re-hit on a downed pedestrian: kept (GTA-like — running over a body hurts again); not gated further.
- 2026-09-24: code review PASS with 5 minors; orchestrator fixes all now (owner delegates, and T15 builds on this): (1) car-car impact written for both cars, (2) damper on compression rate (standard raycast-vehicle model), (3) enter only at <= exit_max_speed, (4) NPC sight/fire-line treat GameLayer::Vehicle as a blocker (else NPCs waste shots into cars; T15 chases), (5) VehicleImpact on pedestrian hits (sound + shake). Plus the three missing gates (sleeping car enter-and-drive, car-car collision, blocked-door exit).
- 2026-09-24: owner playtest report: at speed the bumper catches the 0.15 m curb, takes damage and bounces. Added to the running fixer as a binding priority item: reproduce headless, fix the collider shape/clearance (chamfered nose, higher chassis collider), low steps are never damaging impacts, gate 'curb at 20 m/s = 0 damage, no rebound'.
- 2026-09-25: fixer accepted (curb: convex-hull underbody lift 0.2 + chamfer, 0 damage in 36/36; scrape rule on the car's down axis; sight through cars skips the car containing either endpoint; movement keeps walls-only). Decided the open damper item: cap the compression-rate term (new sedan.ron max_damper_speed, standard clamp) so a curb step is not a 28.7 kN kick — target peak near the old ~7 kN while the settle gates stay green. Also fix now (not T15): exit next to a wall lower than ~3.1 m can put the player on top of it — exit candidates must be at ground level with headroom. Small fixer round 2, then QA.
- 2026-09-25: owner asked for ramps → TASK-027 (full). Owner's wanted question → TASK-026 (small-fix). Order after TASK-015: TASK-026, then T15 (TASK-016), T16 (TASK-017).
- 2026-09-25: owner: ramps 'пока не надо' → TASK-027 parked in blocked/.
- 2026-09-25: owner requested release CI (Linux+Windows via GitHub Actions) after T16 → TASK-028 pending, blocked by TASK-017.
- 2026-09-25: owner wants test CI + README badges too ('чтобы не ждать прогонов локально'). Split: TASK-029 test CI + badges runs right after TASK-026 (helps T15/T16 immediately); TASK-028 release CI stays after T16 and reuses TASK-029's setup.
- 2026-09-25: fixer round 2 finished its work (FIX_SUMMARY complete) but the session ended before hand-back; accepted: max_damper_speed 0.5 (peak 28.4 → 8.2 kN; the spring alone is 6.9 kN, so 7 kN is unreachable by a cap), exit feet-height rule with the roof candidate kept. Forced eject onto a low wall on Wasted/Busted → T15 note.
- 2026-09-25: QA NEEDS_FIXES only for B1 (t13.py static SoundClass list broken by the new Engine class). Tiny fixer: t13 derives classes/caps from mix.ron instead of a hard-coded list; B2 (exit path through a thin fence) fixed now with a capsule cast seat→door. O1/O2 and forced-eject wall carried into TASK-016 (T15) as binding notes.
- 2026-09-25: fixer round 3 accepted (t13 derives classes from the SoundClass enum + mix.ron; exit sweep seat→door). t13 click flake 2/3 (one OS click lost, magazine 12→7) is harness input timing → carried to TASK-017 (T16) with the t9 flake: runtime scenario robustness. SHIP.
- 2026-09-25: PCTX folded: Tnua return bug, pre-step velocities, per-entity ColliderDisabled, curb/speculative margin, sight through cars → bevy-ecs; query order, latch flips, enum lists in scenarios → gates.
